/**
 * RestaurantPress Admin JS
 */
jQuery( function ( $ ) {

	// Tooltips
	var tiptip_args = {
		'attribute': 'data-tip',
		'fadeIn': 50,
		'fadeOut': 50,
		'delay': 200
	};
	$( '.tips, .help_tip, .restaurantpress-help-tip' ).tipTip( tiptip_args );

});
